<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Package visibility (API 30+, enforced when targetSdk >= 30).
         Without this, getInstalledPackages() returns only our own package and
         doesn't see the iFit apps the IfitEcosystemManager checks against.
         https://developer.android.com/training/package-visibility -->
    <queries>
        <package android:name="com.ifit.eru" />
        <package android:name="com.ifit.standalone" />
        <package android:name="com.ifit.arda" />
        <package android:name="com.ifit.glassos_service" />
        <package android:name="com.ifit.gandalf" />
        <package android:name="com.ifit.rivendell" />
        <package android:name="com.ifit.mithlond" />
        <package android:name="com.ifit.launcher" />
    </queries>

    <!-- Bluetooth: dual permission model. API 31+ uses the runtime BLUETOOTH_* perms
         (requested in MainActivity); API <=30 uses the legacy ones. We scan only by the
         Heart Rate service UUID and never derive location, so BLUETOOTH_SCAN is flagged
         neverForLocation and ACCESS_FINE/COARSE_LOCATION is capped at API 30. -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" android:maxSdkVersion="30" />

    <!-- WiFi broadcast. CHANGE_NETWORK_STATE also authorises the connectedDevice
         foreground-service type (see the <service> declaration below). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- USB host -->
    <uses-feature android:name="android.hardware.usb.host" android:required="true" />

    <!-- Overlay window — requires explicit user grant via Settings.canDrawOverlays() on API 23+;
         OverlayManager degrades gracefully when not granted. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Screen sleep — writing Settings.System.SCREEN_OFF_TIMEOUT for the "sleep after idle"
         feature. Special access: granted via Settings.System.canWrite() / the
         ACTION_MANAGE_WRITE_SETTINGS screen on API 23+ (auto-granted at install below that).
         ScreenSleepController degrades gracefully when not granted. -->
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />

    <!-- Foreground service. connectedDevice type needs its own permission on API 34+;
         POST_NOTIFICATIONS is a runtime perm on API 33+ (requested in MainActivity),
         needed so the service notification's updates aren't dropped. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Auto-start on boot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- App self-update — commits the downloaded APK via PackageInstaller (AppInstaller).
         Normal-protection permission, auto-granted at install. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name=".HyperboreaApp"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/Theme.Hyperborea"
        tools:targetApi="tiramisu">
        <!-- screenOrientation is intentional: the console is a fixed-landscape kiosk. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.Hyperborea"
            tools:ignore="DiscouragedApi">
            <!-- singleTask: the equipment's USB re-enumerates every ~5-20s (motor-controller
                 watchdog), and this activity handles USB_DEVICE_ATTACHED, so in the default
                 (standard) launch mode each re-attach spawned a NEW MainActivity on top — a fresh
                 NavHost reset to the dashboard, bouncing the user out of Settings mid-tap. As a
                 single instance, a re-attach just delivers onNewIntent to the live activity
                 (logs it, no navigation reset), so whatever screen the user is on is preserved. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Eligible as the device's home screen, but only as a last resort.
                 On an ICON console the stock home is com.ifit.launcher; the deploy
                 script disables every com.ifit.* package (otherwise com.ifit.launcher
                 re-enables ERU on boot — ERU then crash-loops on USB attach and pushes
                 firmware updates that wipe the device), then hands the HOME intent to
                 the device's own AOSP launcher (com.android.launcher3) via
                 `cmd package set-home-activity` — falling back to this activity only if
                 the console ships no other launcher. Hyperborea runs headless as a
                 foreground service either way (BootReceiver restarts it on boot), so it
                 doesn't need to be the home app. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <!-- Auto-launch on FitPro USB attach so Android offers the "always" checkbox.
                 Once accepted, USB permission persists across the device's ~20s connect cycle. -->
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/usb_device_filter" />
        </activity>

        <service
            android:name=".HyperboreaService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".UpdateRestartReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
