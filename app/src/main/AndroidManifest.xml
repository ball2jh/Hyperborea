<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Package visibility (API 30+, enforced when targetSdk >= 30).
         Without this, getInstalledPackages() returns only our own package and
         doesn't see the iFit apps the IfitEcosystemManager checks against.
         https://developer.android.com/training/package-visibility -->
    <queries>
        <package android:name="com.ifit.eru" />
        <package android:name="com.ifit.standalone" />
        <package android:name="com.ifit.arda" />
        <package android:name="com.ifit.glassos_service" />
        <package android:name="com.ifit.gandalf" />
        <package android:name="com.ifit.rivendell" />
        <package android:name="com.ifit.mithlond" />
        <package android:name="com.ifit.launcher" />
    </queries>

    <!-- BLE peripheral mode (pre-API 31 permission model) -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- WiFi broadcast -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- External storage for log export (auto-granted on API 25) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- USB host -->
    <uses-feature android:name="android.hardware.usb.host" android:required="true" />

    <!-- Overlay window — requires explicit user grant via Settings.canDrawOverlays() on API 23+;
         OverlayManager degrades gracefully when not granted. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Foreground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Auto-start on boot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- App self-update — fires the system installer dialog via ACTION_VIEW.
         Normal-protection permission, auto-granted at install. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name=".HyperboreaApp"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hyperborea">
        <activity
            android:name=".MainActivity"
            android:directBootAware="true"
            android:exported="true"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.Hyperborea">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Auto-launch on FitPro USB attach so Android offers the "always" checkbox.
                 Once accepted, USB permission persists across the device's ~20s connect cycle. -->
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/usb_device_filter" />
        </activity>

        <service
            android:name=".HyperboreaService"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".UpdateRestartReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
